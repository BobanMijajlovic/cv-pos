import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  root: {},
  absolute: {
    position: 'absolute',
    top: 55,
    width: '100%',
    zIndex: 1,
  },
});

export default style;
